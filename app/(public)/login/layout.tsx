export const metadata = {
  title: "Diloi Finance App - Login",
  description: "Diloi Finance App",
};

const RootLayout = ({ children }: { children: React.ReactNode }) => (
  <>{children}</>
);

export default RootLayout;
